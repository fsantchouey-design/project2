<style>
  .cs-page {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 60px 24px;
    background: #080f1a;
  }
  .cs-card {
    text-align: center;
    max-width: 520px;
    width: 100%;
    background: rgba(15,23,42,0.9);
    border: 1px solid rgba(20,184,166,0.3);
    border-radius: 24px;
    padding: 56px 40px;
    box-shadow: 0 0 80px rgba(20,184,166,0.08);
  }
  .cs-icon {
    width: 72px;
    height: 72px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(20,184,166,0.2), rgba(56,189,248,0.15));
    border: 1px solid rgba(20,184,166,0.4);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 28px;
  }
  .cs-icon i { width: 34px; height: 34px; color: #14b8a6; }
  .cs-title {
    font-size: 1.9rem;
    font-weight: 900;
    color: #f1f5f9;
    margin: 0 0 14px;
    letter-spacing: -0.02em;
  }
  .cs-sub {
    font-size: 1rem;
    color: #64748b;
    line-height: 1.65;
    margin: 0 0 36px;
  }
  .cs-actions {
    display: flex;
    flex-direction: column;
    gap: 12px;
    align-items: center;
  }
  .cs-btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 14px 32px;
    border-radius: 14px;
    background: linear-gradient(135deg, #14b8a6, #2563eb);
    color: #fff;
    font-weight: 800;
    font-size: 0.95rem;
    text-decoration: none;
    transition: opacity 0.2s, transform 0.2s;
  }
  .cs-btn-primary:hover { opacity: 0.88; transform: translateY(-1px); }
  .cs-btn-secondary {
    font-size: 0.85rem;
    color: #475569;
    text-decoration: none;
  }
  .cs-btn-secondary:hover { color: #94a3b8; }
</style>

<div class="cs-page">
  <div class="cs-card">
    <div class="cs-icon">
      <i data-lucide="check-circle"></i>
    </div>
    <h1 class="cs-title">Paiement confirmé !</h1>
    <p class="cs-sub">
      Votre abonnement CraftyCrib est maintenant actif.<br>
      Vos crédits IA sont prêts à être utilisés.
    </p>
    <div class="cs-actions">
      <a href="/dashboard" class="cs-btn-primary">
        <i data-lucide="layout-dashboard" style="width:17px;height:17px;"></i>
        Accéder à mon espace
      </a>
      <a href="/pricing" class="cs-btn-secondary">Retour aux tarifs</a>
    </div>
  </div>
</div>

<script>
(function () {
  if (typeof lucide !== 'undefined') lucide.createIcons();
})();
</script>
